apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-config
  namespace: default
data:
  POSTGRES_HOST: "ai-postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_USER: "postgres"
  POSTGRES_DATABASE: "schema_agent"
